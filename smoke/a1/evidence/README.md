# Evidence files

This directory contains the unmodified text logs, generated training arguments,
and CSV metrics collected from the A1 smoke on `scut-server`.

The generated model weights are deliberately not versioned here. Their paths,
sizes, and SHA-256 values are recorded in the top-level Smoke report and can
be regenerated using `scripts/reproduce/run_a1_smoke.sh`.
